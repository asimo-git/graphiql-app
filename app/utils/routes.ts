export enum Routes {
  Home = '/',
  Authentication = '/authentication',
  Registration = '/registration',
  RESTfull = '/RESTfull',
  GraphiQL = '/GraphiQL',
  History = '/history',
}

export default Routes;
